# Download multiple URLs links at once

Edit parameters as needed in the script, copy and paste it into browser console to run it.

Note: Uses the download attribute which only works on same-origin URLs.